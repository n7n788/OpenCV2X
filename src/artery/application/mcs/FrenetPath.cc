#include "FrenetPath.h"
#include <vector>

namespace artery
{

FrenetPath::generateFrenetPath(const QuinticPolynomial& qp) {
	// 位置情報を生成
	for (double t = 0.0; double < TIME_LENGTH; double += TIME_STEP) {
		mLon	
	}
}

